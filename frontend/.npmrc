# EAS Build / npm compatibility.
# Forces npm to fall back to legacy peer-dep resolution so hoisted peer
# ranges (e.g. @react-navigation/native 7.1.14 vs 7.1.8) don't break the
# Install Dependencies phase on EAS cloud builders.
legacy-peer-deps=true
